jwt.secret : khawarmaliik

spring.jpa:
  database: MYSQL
  hibernate.ddl-auto: update
  show-sql: true

spring.datasource:
  platform: mysql
  driverClassName: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://localhost:3306/database-properties
  username: root
  password: malik123
  

server:
  port: 8085

---
spring:
  application:
    name: Authentication

server:
  servlet:
    context-path: /Authentication